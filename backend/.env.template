DB_USER=user
DB_PASSWORD=user_password
DB_NAME=database_name

SMTP_USER=email
SMTP_PASSWORD=password
SMTP_HOST=host
SMTP_PORT=0

FRONTEND_URL=url
CSRF_TOKEN=token

PROFILER_USER=user
PROFILER_PASSWORD=user_password

# Celery использует БД 0
REDIS_BROKER=route
REDIS_BACKEND=route

# Rate Limiting использует БД 1
REDIS_CLIENT=route

# Кэш для SQLAlchemy данных использует БД 2
REDIS_SESSIONS=route

IS_PROD=is_prod_True/False_?
